#include <iostream>
#include <cstdlib> // atoi
#include "MaxStack.h"

using namespace std;

int main(int argc, char *argv[]){
    MaxStack stack;

    /**MaxStack stack;

     for(int i = 1; i < argc; i++){
          if (argv[i][0] == '.'){
              cout << stack.top() << endl;
              stack.pop();
          } else {
              stack.push(atoi(argv[i]));
          }
      } */

    return 0;
}